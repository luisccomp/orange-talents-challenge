spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vaccinedb?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: docker@1993#

  jpa:
    properties:
      hibernate:
        enable_lazy_load_no_trans: true